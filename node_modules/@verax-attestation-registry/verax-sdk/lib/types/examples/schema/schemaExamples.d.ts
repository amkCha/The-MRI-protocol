import { VeraxSdk } from "../../src/VeraxSdk";
export default class SchemaExamples {
    private veraxSdk;
    constructor(_veraxSdk: VeraxSdk);
    run(argv: string, methodName?: string, waitForConfirmation?: boolean): Promise<void>;
}
//# sourceMappingURL=schemaExamples.d.ts.map